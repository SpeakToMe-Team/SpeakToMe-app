<% layout('layout') %>

<div id="app" class="welcome-page">
    <div class="welcome-container">

        <% if (success_msg.length > 0){ %>
            <div class="alert alert-success"><%= success_msg %></div>
        <% } %>

        <div> 
            <img class="logo animated bounceInDown" src="/images/microphone.png">
        </div>
        <h1 class="animated rollIn">{{ title }}</h1>

        <div>
            <img id="btn-google-auth" class="authen animated fadeIn" src="/images/googleAuth.png" alt="google authentificator">
        </div>

        <div>
            <a href="/auth/google" class="btn btn-danger authen animated fadeIn"><span class="fa fa-google"></span> Google</a>
        </div>

        <div>
            <a href="/auth/facebook" class="btn btn-primary"><span class="fa fa-facebook"></span> Facebook</a>
        </div>
        

            <p>Login or Register with :</p>
            <a href="/auth/login" class="btn btn-default">
                <span class="fa fa-user"></span> Local Login
            </a>
            <a href="/auth/signup" class="btn btn-default">
                <span class="fa fa-user"></span> Local signup
            </a>
            <!--<a href="/auth/facebook" class="btn btn-primary">
                <span class="fa fa-facebook"></span> Facebook
            </a>
            <a href="/auth/google" class="btn btn-danger">
                <span class="fa fa-google"></span> Google
            </a>-->
                

        
    </div>

</div>


<script>
    var app = new Vue({
        el: "#app",
        data: {
            title: "SpeakToMe"
        },
        methods: {
        }
    });
</script>